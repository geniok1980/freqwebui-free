# Freqdash API credentials (bot needs these to read settings)
FREQDASH_API_URL=http://backend:8000/api/v1
FREQDASH_USERNAME=admin
FREQDASH_PASSWORD=admin
FREQDASH_TENANT_SLUG=default

# NOTE: TELEGRAM_BOT_TOKEN and ALLOWED_USER_IDS are configured
# via the web UI: Settings → Telegram Bot.
# The bot picks up changes automatically every 30 seconds.
